# Build / artifacts
**/dist/
**/build/
**/coverage/
**/tmp/
**/node_modules/
**/.vite/
.local/
APROOF/.local/
# Devnet keypairs and local DB — never commit secrets or PGlite files.
*.key
*.pem
*keypair*.json
zerion-agent-keypair.json
solana-devnet-keypair.json
anchor-devnet.json
*.log
*.tmp
logs/
data/
APROOF/data/

# Local env (untracked OK — release:preflight fails only on git-tracked secrets; see README)
.env
.env.*
!.env.example
!.env.demo.example
*.keypair.json
anchor-devnet.json
wallets/
secrets/

# Local DB / cache (if created at repo root)
*.sqlite
*.sqlite-journal
*.db

# OS
.DS_Store
Thumbs.db

# Deprecated / backup — not source of truth
_fe_api_hooks_backup/
