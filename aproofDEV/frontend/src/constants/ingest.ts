/**
 * Default `source_type_key` provisioned server-side when the first subject is created for an
 * org+environment with no mapping rules (`APROOF_DEFAULT_INGEST_SOURCE_TYPE_KEY` in subject-service).
 */
export const APROOF_DEFAULT_INGEST_SOURCE_TYPE_KEY = "aproof.default.action_completed";
